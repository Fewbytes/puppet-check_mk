ipaddresses["<%= scope.lookupvar("fqdn") -%>"] = "<%= scope.lookupvar("ipaddress") -%>"
<% tags = host_tags + classes %>
all_hosts += [
    "<%= scope.lookupvar("fqdn") -%>|<%= tags.sort.join('|') %>",
]